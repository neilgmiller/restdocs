---
phase: 01-storage-model-validation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/giffardtechnologies/restdocs/storage/Method.kt
  - src/main/java/com/giffardtechnologies/restdocs/domain/Method.kt
  - src/main/java/com/giffardtechnologies/restdocs/mappers/StorageToDomainMappers.kt
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - VALID-01
  - VALID-02

must_haves:
  truths:
    - "A YAML method entry containing asyncResponse parses without UnrecognizedPropertyException"
    - "After parsing and mapping, domain.Method.asyncResponse is non-null for methods that declare the block and null for methods that do not"
    - "A malformed asyncResponse (invalid fields, unresolved typeref) produces a validation error — same rules as response"
  artifacts:
    - path: "src/main/java/com/giffardtechnologies/restdocs/storage/Method.kt"
      provides: "storage.Method data class with asyncResponse field"
      contains: "val asyncResponse: Response? = null"
    - path: "src/main/java/com/giffardtechnologies/restdocs/domain/Method.kt"
      provides: "domain.Method class with asyncResponse field"
      contains: "var asyncResponse: Response? = null"
    - path: "src/main/java/com/giffardtechnologies/restdocs/mappers/StorageToDomainMappers.kt"
      provides: "MethodStorageModel.mapToModel wires asyncResponse to domain"
      contains: "asyncResponse = asyncResponse?.mapToModel(context)"
  key_links:
    - from: "storage.Method.asyncResponse"
      to: "domain.Method.asyncResponse"
      via: "MethodStorageModel.mapToModel(context)"
      pattern: "asyncResponse = asyncResponse\\?\\.mapToModel\\(context\\)"
    - from: "YAML asyncResponse key"
      to: "storage.Method.asyncResponse"
      via: "Jackson data class binding"
      pattern: "val asyncResponse: Response\\? = null"
---

<objective>
Add the asyncResponse field to both storage and domain Method models and wire it through the StorageToDomainMappers pipeline.

Purpose: Jackson 3.x FAIL_ON_UNKNOWN_PROPERTIES is true and not overridden — YAML files with asyncResponse will throw UnrecognizedPropertyException until storage.Method declares the field. This plan creates that prerequisite and connects it all the way to domain.Method. Structural validation of asyncResponse contents (VALID-02) is satisfied for free because ResponseStorageModel.mapToModel(context) is reused verbatim.

Output: Three files modified. The asyncResponse field is parseable, mappable, and structurally validated with zero new validation code.
</objective>

<execution_context>
@/Users/nmiller/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nmiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nmiller/Dev/Personal Projects/restdocs/.planning/ROADMAP.md
@/Users/nmiller/Dev/Personal Projects/restdocs/.planning/REQUIREMENTS.md
@/Users/nmiller/Dev/Personal Projects/restdocs/.planning/STATE.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from live codebase. -->

From src/main/java/com/giffardtechnologies/restdocs/storage/Method.kt (lines 36-55):
The data class constructor ends with response at line 50, then the two @JsonProperty-annotated code fields:
  val response: Response? = null,
  @JsonProperty("successful codes")
  val successCodes: ArrayList<String> = ArrayList(),
  @JsonProperty("failure codes")
  val failureCodes: ArrayList<String> = ArrayList(),
Insert asyncResponse immediately after response (no @JsonProperty annotation — key name matches field name exactly).

From src/main/java/com/giffardtechnologies/restdocs/domain/Method.kt (lines 6-20):
  var response: Response? = null,
  var requestBody: RequestBody? = null,
Insert asyncResponse immediately after response with the same var + nullable + default null pattern.

From src/main/java/com/giffardtechnologies/restdocs/mappers/StorageToDomainMappers.kt (line 517):
  response = response?.mapToModel(context),
Add the following line immediately after:
  asyncResponse = asyncResponse?.mapToModel(context),
The ResponseStorageModel.mapToModel(context) extension function (lines 534-539) runs full structural validation — no changes needed to it.

From src/main/java/com/giffardtechnologies/restdocs/storage/Response.kt:
storage.Response is the existing type for the asyncResponse field. domain.Response is the existing domain counterpart. Both are reused verbatim (per D-08) — no new types.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add asyncResponse field to storage.Method</name>
  <files>src/main/java/com/giffardtechnologies/restdocs/storage/Method.kt</files>
  <read_first>
    src/main/java/com/giffardtechnologies/restdocs/storage/Method.kt
  </read_first>
  <action>
    In the storage.Method data class constructor, insert the parameter `val asyncResponse: Response? = null` immediately after `val response: Response? = null` (line 50). No @JsonProperty annotation — the YAML key asyncResponse matches the Kotlin property name exactly (per D-08, reuses storage.Response verbatim). The KDoc comment block above the class does not need to reference asyncResponse in this phase.
  </action>
  <verify>
    <automated>cd "/Users/nmiller/Dev/Personal Projects/restdocs" && ./gradlew compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - storage/Method.kt contains the line `val asyncResponse: Response? = null` in the data class constructor body, positioned after `val response: Response? = null`
    - No @JsonProperty annotation appears on the asyncResponse field
    - ./gradlew compileKotlin exits 0
  </acceptance_criteria>
  <done>storage.Method compiles with asyncResponse field; Jackson can bind the YAML key to it without UnrecognizedPropertyException</done>
</task>

<task type="auto">
  <name>Task 2: Add asyncResponse field to domain.Method and wire mapper</name>
  <files>
    src/main/java/com/giffardtechnologies/restdocs/domain/Method.kt
    src/main/java/com/giffardtechnologies/restdocs/mappers/StorageToDomainMappers.kt
  </files>
  <read_first>
    src/main/java/com/giffardtechnologies/restdocs/domain/Method.kt
    src/main/java/com/giffardtechnologies/restdocs/mappers/StorageToDomainMappers.kt
  </read_first>
  <action>
    In domain/Method.kt: insert `var asyncResponse: Response? = null` immediately after `var response: Response? = null` (line 16). This is a plain class (not a data class) — use var with a null default so existing positional-free call sites require no changes (per D-08).

    In StorageToDomainMappers.kt: in the MethodStorageModel.mapToModel(context) function body (around line 506), add the named argument `asyncResponse = asyncResponse?.mapToModel(context)` immediately after the `response = response?.mapToModel(context)` line. The safe-call (?.) ensures null storage input produces null domain output. No changes to ResponseStorageModel.mapToModel — it is reused verbatim for asyncResponse, which satisfies VALID-02 for free.
  </action>
  <verify>
    <automated>cd "/Users/nmiller/Dev/Personal Projects/restdocs" && ./gradlew build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - domain/Method.kt contains `var asyncResponse: Response? = null` immediately after `var response: Response? = null`
    - StorageToDomainMappers.kt contains `asyncResponse = asyncResponse?.mapToModel(context)` as a named argument in the Method(...) constructor call inside MethodStorageModel.mapToModel
    - ./gradlew build exits 0 (compiles and all existing tests pass)
  </acceptance_criteria>
  <done>Full storage-to-domain pipeline connected for asyncResponse; ./gradlew build passes; FOUND-01, FOUND-02, FOUND-03, VALID-01, VALID-02 are satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file → Jackson | Untrusted input: YAML files may contain unexpected keys or malformed asyncResponse blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | YAML asyncResponse key | mitigate | FAIL_ON_UNKNOWN_PROPERTIES=true (existing); adding storage field is the mitigation — unknown keys still throw for anything else |
| T-01-02 | Denial of Service | Deeply nested asyncResponse | accept | Same risk profile as existing response field; no additional mitigation warranted |
</threat_model>

<verification>
./gradlew build exits 0 after both tasks.

Manual smoke check (optional): add a YAML method entry with `asyncResponse: {type: object, fields: []}` to any test fixture and confirm no UnrecognizedPropertyException is thrown when parsed with createMapper().
</verification>

<success_criteria>
- storage.Method.asyncResponse: Response? = null field present in data class constructor
- domain.Method.asyncResponse: Response? = null field present in plain class constructor
- StorageToDomainMappers.kt MethodStorageModel.mapToModel contains asyncResponse = asyncResponse?.mapToModel(context)
- ./gradlew build passes with no new failures
</success_criteria>

<output>
Create `.planning/phases/01-storage-model-validation-foundation/01-01-SUMMARY.md` when done
</output>
