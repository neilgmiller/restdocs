---
phase: 01-storage-model-validation-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/main/java/com/giffardtechnologies/restdocs/storage/Method.kt
  - src/main/java/com/giffardtechnologies/restdocs/DocValidator.kt
autonomous: true
requirements:
  - VALID-03
  - VALID-04

must_haves:
  truths:
    - "A YAML method with asyncResponse but no job field in response produces a validation error with the method name"
    - "A YAML method whose response has a job field but has no asyncResponse produces a warning via messageHandler"
    - "Methods with neither condition produce no error or warning"
    - "The accumulatingContext pass does not spuriously fire VALID-03 or VALID-04"
  artifacts:
    - path: "src/main/java/com/giffardtechnologies/restdocs/storage/Method.kt"
      provides: "2-arg validate override with VALID-03/04 cross-field checks and responseHasJobField helper"
      contains: "override fun validate(validationContext: Any?, warningEmitter: (String) -> Unit)"
    - path: "src/main/java/com/giffardtechnologies/restdocs/DocValidator.kt"
      provides: "Both createMapper calls receive warningEmitter routed through messageHandler"
      contains: "val warningEmitter: (String) -> Unit = { message -> messageHandler(message) }"
  key_links:
    - from: "storage.Method.validate(fullContext, warningEmitter)"
      to: "ValidationException (VALID-04)"
      via: "responseHasJobField() returns false and asyncResponse != null"
      pattern: "asyncResponse != null && !responseHasJobField\\(response\\)"
    - from: "storage.Method.validate(fullContext, warningEmitter)"
      to: "warningEmitter call (VALID-03)"
      via: "responseHasJobField() returns true and asyncResponse == null"
      pattern: "responseHasJobField\\(response\\) && asyncResponse == null"
    - from: "DocValidator.getValidatedDocument"
      to: "createMapper(AccumulatingContext, warningEmitter)"
      via: "warningEmitter val defined from messageHandler"
      pattern: "createMapper\\(AccumulatingContext.*warningEmitter\\)"
---

<objective>
Implement VALID-03 (warning) and VALID-04 (error) cross-field validation in storage.Method, and wire DocValidator to pass warnings back through its messageHandler callback.

Purpose: The warning infrastructure (Plan 02) is in place; the asyncResponse field exists (Plan 01). This plan activates the cross-field logic. storage.Method.validate() overrides the 2-arg form and applies the responseHasJobField helper to gate both checks. DocValidator routes the emitter through its existing messageHandler so warnings are caller-visible. The AccumulatingContext guard ensures the typeRef checks only run when full type information is available.

Output: Two files modified. VALID-03 and VALID-04 become operational.
</objective>

<execution_context>
@/Users/nmiller/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nmiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nmiller/Dev/Personal Projects/restdocs/.planning/ROADMAP.md
@/Users/nmiller/Dev/Personal Projects/restdocs/.planning/REQUIREMENTS.md
@/Users/nmiller/Dev/Personal Projects/restdocs/.planning/STATE.md
@/Users/nmiller/Dev/Personal Projects/restdocs/.planning/phases/01-storage-model-validation-foundation/01-01-SUMMARY.md
@/Users/nmiller/Dev/Personal Projects/restdocs/.planning/phases/01-storage-model-validation-foundation/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from live codebase + Plan 01/02 changes. -->

From src/main/java/com/giffardtechnologies/restdocs/storage/Method.kt (existing validate, lines 61-76):
  override fun validate(validationContext: Any?) {
      if (path == null && id == null) {
          throw ValidationException("A method must have at least one of 'id' and 'path'")
      }
      if (validationContext is DocValidator.AccumulatingContext) {
          if (validationContext.methodClassNames.contains(name)) {
              throw ValidationException("A method already exists with the name: \"$name\"")
          } else {
              parameters?.validateHasNoDuplicates()
              validationContext.methodClassNames.add(name)
          }
      } else {
          parameters?.validateHasNoDuplicates(validationContext.documentIfAvailable)
      }
  }

Add immediately below this existing 1-arg override:
  override fun validate(validationContext: Any?, warningEmitter: (String) -> Unit)
  private fun responseHasJobField(response: Response?): Boolean

Import already present at line 8: import com.giffardtechnologies.restdocs.storage.type.Field
Import already present at line 4: import com.giffardtechnologies.restdocs.DocValidator

From src/main/java/com/giffardtechnologies/restdocs/storage/Response.kt:
  The fields property is ArrayList<FieldListElement>? inherited via TypeSpec.
  FieldListElement is abstract. Field and FieldListIncludeElement are concrete subtypes.
  Field has a name: String property. FieldListIncludeElement does not — check `it is Field` before accessing .name.

From src/main/java/com/giffardtechnologies/restdocs/DocValidator.kt (getValidatedDocument, lines 34-61):
  Two createMapper call sites:
    line 38: val mapper = createMapper(AccumulatingContext(validationOptions))
    line 52: val contextMapper = createMapper(FullContext(referencableTypes, document, validationOptions))
  messageHandler: (String) -> Unit = {} is already a parameter to getValidatedDocument.
  Route warningEmitter through messageHandler (per D-03 planner discretion — keeps warnings caller-visible).

From Validatable.kt (after Plan 02):
  fun validate(validationContext: Any?, warningEmitter: (String) -> Unit)
  is now a default interface method — override it in storage.Method.

D-07 definition of "has a job field":
  response.typeRef in {"AsyncJobResponse", "AsyncCapableResponse"} — string comparison only, no type resolution needed
  OR response.fields contains an element where `it is Field && it.name == "job"`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add validate 2-arg override and responseHasJobField to storage.Method</name>
  <files>src/main/java/com/giffardtechnologies/restdocs/storage/Method.kt</files>
  <read_first>
    src/main/java/com/giffardtechnologies/restdocs/storage/Method.kt
    src/main/java/com/giffardtechnologies/restdocs/storage/Response.kt
  </read_first>
  <action>
    In storage/Method.kt, add two new functions after the existing 1-arg `validate(validationContext: Any?)` override:

    1. `override fun validate(validationContext: Any?, warningEmitter: (String) -> Unit)` — the body must:
       a. Call `validate(validationContext)` first to run all existing 1-arg checks unchanged.
       b. Then gate on `if (validationContext !is DocValidator.AccumulatingContext)` — VALID-03 and VALID-04 checks run ONLY in the else branch (FullContext pass), matching the existing AccumulatingContext gate pattern in the 1-arg override (per D-04 and the anti-pattern note about Pitfall 2).
       c. Inside that gate: check VALID-04 first — `if (asyncResponse != null && !responseHasJobField(response))` then throw `ValidationException("Method '$name': asyncResponse is present but response has no 'job' field")` (per D-05).
       d. Then check VALID-03 — `if (responseHasJobField(response) && asyncResponse == null)` then call `warningEmitter("WARNING: Method '$name': response has a job field but no asyncResponse block")` (per D-06).

    2. `private fun responseHasJobField(response: Response?): Boolean` — the body:
       a. Return false if response is null.
       b. Return true if `response.typeRef` is in the set `setOf("AsyncJobResponse", "AsyncCapableResponse")` (per D-07 typeRef check).
       c. Otherwise return `response.fields?.any { it is Field && it.name == "job" } == true` — the `is Field` check is mandatory because FieldListElement is abstract with Field and FieldListIncludeElement as subtypes; FieldListIncludeElement has no name property (per Pitfall 3).
       The `Field` import (`com.giffardtechnologies.restdocs.storage.type.Field`) is already present at line 8 — no new import needed.
  </action>
  <verify>
    <automated>cd "/Users/nmiller/Dev/Personal Projects/restdocs" && ./gradlew compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - storage/Method.kt contains `override fun validate(validationContext: Any?, warningEmitter: (String) -> Unit)` as a method of the Method data class
    - The 2-arg override body calls `validate(validationContext)` as the first statement
    - The VALID-04 and VALID-03 checks are inside `if (validationContext !is DocValidator.AccumulatingContext)` — NOT in the else branch of the AccumulatingContext check, but as an independent guard after the super-call
    - storage/Method.kt contains `private fun responseHasJobField(response: Response?): Boolean`
    - The responseHasJobField body uses `it is Field && it.name == "job"` (not a bare `.name` on FieldListElement)
    - ./gradlew compileKotlin exits 0
  </acceptance_criteria>
  <done>storage.Method overrides 2-arg validate with VALID-03/04 logic; responseHasJobField helper correctly handles all FieldListElement subtypes</done>
</task>

<task type="auto">
  <name>Task 2: Wire warningEmitter in DocValidator</name>
  <files>src/main/java/com/giffardtechnologies/restdocs/DocValidator.kt</files>
  <read_first>
    src/main/java/com/giffardtechnologies/restdocs/DocValidator.kt
  </read_first>
  <action>
    In DocValidator.getValidatedDocument, define a warningEmitter lambda once and pass it to both createMapper calls:

    1. After the `println("Validating...")` call and before the first createMapper call, add: `val warningEmitter: (String) -> Unit = { message -> messageHandler(message) }`. This routes warnings through the existing messageHandler parameter, keeping warnings caller-visible (per D-03 planner discretion — messageHandler over System.err).

    2. Update the first createMapper call (line 38) from `createMapper(AccumulatingContext(validationOptions))` to `createMapper(AccumulatingContext(validationOptions), warningEmitter)`.

    3. Update the second createMapper call (line 52) from `createMapper(FullContext(referencableTypes, document, validationOptions))` to `createMapper(FullContext(referencableTypes, document, validationOptions), warningEmitter)`.

    No other changes to DocValidator. The validate() call on line 30 that routes to println is unrelated to warningEmitter.
  </action>
  <verify>
    <automated>cd "/Users/nmiller/Dev/Personal Projects/restdocs" && ./gradlew build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - DocValidator.kt contains `val warningEmitter: (String) -> Unit = { message -> messageHandler(message) }` inside getValidatedDocument
    - Both createMapper calls in getValidatedDocument include warningEmitter as the second argument
    - ./gradlew build exits 0 (all existing tests pass)
  </acceptance_criteria>
  <done>DocValidator routes warningEmitter through messageHandler; VALID-03 warnings will reach DocValidator callers; ./gradlew build passes; all Phase 1 requirements are implemented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage.Method.validate() -> ValidationException | Error message includes method name from YAML — internal toolchain data, not user-visible runtime output |
| storage.Method.validate() -> warningEmitter | Warning string includes method name from YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | ValidationException message | accept | Messages contain YAML method names; toolchain CLI tool — not a web API; output is expected diagnostic info |
| T-03-02 | Denial of Service | VALID-04 throws on second pass | accept | Intentional behavior; one invalid method stops the second-pass run — consistent with existing validation fail-fast pattern |
</threat_model>

<verification>
./gradlew build exits 0 after both tasks.

End-to-end Phase 1 checks (all five success criteria from ROADMAP.md Phase 1):
1. A YAML method with `asyncResponse: {type: object, fields: []}` parses without error — confirmed by Plan 01.
2. domain.Method.asyncResponse is non-null after mapping for methods with the block — confirmed by Plan 01.
3. A malformed asyncResponse produces a validation error — confirmed by Plan 01 (ResponseStorageModel.mapToModel reuse).
4. A YAML method with asyncResponse but no job field in response produces a ValidationException with the method name — VALID-04 check.
5. A YAML method whose response has a job field but no asyncResponse produces a warning via messageHandler — VALID-03 check.
</verification>

<success_criteria>
- storage.Method has 2-arg validate override that calls validate(validationContext) first, then gates VALID-03/VALID-04 on !AccumulatingContext
- responseHasJobField helper uses `it is Field && it.name == "job"` for inline field list check
- DocValidator passes warningEmitter (routed via messageHandler) to both createMapper calls
- ./gradlew build passes with no new failures
- All five ROADMAP Phase 1 success criteria are satisfied
</success_criteria>

<output>
Create `.planning/phases/01-storage-model-validation-foundation/01-03-SUMMARY.md` when done
</output>
